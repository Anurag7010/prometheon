// Stub for the `server-only` package — no-op in test environments.
// Next.js uses this to guard Server Component modules from client imports.
// Tests run in vitest/jsdom and don't enforce this boundary.
export {}
